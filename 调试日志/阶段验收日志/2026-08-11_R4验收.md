# R4 验收记录（2026-08-11）

## 判定

**R4 GATE: PASS** — 可进入 R5（reset/home）。

说明：物理急停由现场按键负责，**未纳入本阶段软件测试**；软件路径验证了 `close()` → `stop` + `clear`。

```bash
cd /home/naviai/hilserl_orin
docker exec -i hilserl bash -lc '
source /opt/conda/etc/profile.d/conda.sh && conda activate hil-actor &&
source /ros_noetic/catkin_ws/devel/setup.bash &&
source /opt/ros/noetic/setup.bash --extend &&
source /root/catkin_ws/devel/setup.bash &&
export PYTHONPATH=/root/catkin_ws/src:$PYTHONPATH &&
cd /root/catkin_ws &&
python -m unittest discover -s src/hilserl_wa2/tests/unit -v &&
python src/hilserl_wa2/scripts/verify_r2_mock_env.py &&
python src/hilserl_wa2/scripts/verify_r3_readonly_env.py &&
python src/hilserl_wa2/scripts/verify_r4_servo_dryrun.py &&
R4_CONFIRM=YES python src/hilserl_wa2/scripts/verify_r4_servo_gates.py --gate all
'
```

结果摘要：

- 全量单测：**54 passed**
- R2 / R3 / R4 dry-run：**PASS**
- 真机 Gates：hold / XYZ±1mm / RPY 累计±2° / clip / software crash_stop → **R4 GATES: PASS**
- Hold 漂移约 `2e-5 m`；开跑前 `left_arm_is_singular=False`

## 核心产出

| 路径 | 含义 |
|---|---|
| `ros_adapters/servo_session.py` | 唯一 ServoL 执行器；限幅、workspace、stale/singular → stop+clear |
| `envs/wa2_env.py` | 支持 `read_only=False` 真机 step |
| `tests/unit/test_servo_session.py` | 离线限幅 / dry-run / stale / box |
| `scripts/verify_r4_servo_dryrun.py` | 不算真机的目标计算验收 |
| `scripts/verify_r4_servo_gates.py` | 真机分层 Gate（需 `R4_CONFIRM=YES`） |
| `scripts/r4_hold_zero_action.py` | 零动作保持 |
| `调试日志/阶段验收日志/2026-08-11_R4_gates.txt` | Gate CSV |
| `调试日志/阶段验收日志/2026-08-11_R4验收_raw.txt` | 回归原始输出 |

## 行为冻结

- 真机运动需 `R4_CONFIRM=YES`
- 单步 ≤1 mm / 0.25°；±2° 由多步累计
- 异常/close：软件 `stop`+`clear`（物理急停不测）
- 图像仍 Mock
