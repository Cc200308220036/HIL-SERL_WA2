# R10 验收记录与阶段成果总结

> 日期：2026-08-14  
> 判定：**R10 GATE: PASS**  
> 方案：`docs/solution/R10_方案.md`；联调手册：`HILSERL_Learner/docs/R10_Learner联调手册.md`  
> 前置：`调试日志/阶段验收日志/2026-08-13_R9验收.md` → **R9 GATE: PASS**  
> 运行 ID：`20260814_153513_r10`  
> Learner 证据：`HILSERL_Learner/runs/wa2_bottle_pick/20260814_153513_r10/`  
> Actor 证据（笔记本同步树）：`catkin_ws/runs/wa2_bottle_pick/20260814_153513_r10/`  
> （Orin 容器内等价路径：`/root/catkin_ws/runs/wa2_bottle_pick/20260814_153513_r10/`）  
> 预检：`HILSERL_Learner/runs/r10_preflight/`；Actor 预检：`catkin_ws/runs/wa2_bottle_pick/r10_preflight/`  
> 任务：`bottle_pick` / `wa2_bottle_pick`  
> Learner：笔记本 `172.16.9.36` / Conda `hil-learner` / Python `3.10.20` / 根 `/home/cyw/orin_hilserl/HILSERL_Learner`  
> Actor：Orin `hilserl` / `hil-actor` / `172.16.8.58` / 根 `/root/catkin_ws`

---

## 1. 一句话结论

Orin Actor ↔ 笔记本 Learner 跨机链路已打通：handshake、1020/20 双 store、params 广播、断线 fail-closed、人工新 session 恢复、readonly 真观测与 live-zero 断线 `stop+clear` 均有 PASS 证据；**未训练 SAC 未控真机，R10 不代表 RLPD/demo/classifier 已完成**。

```text
R10 GATE: PASS
```

复验依据：同一 `R10_RUN_ID` 下 Learner/Actor 日志与 JSON 关键字齐全；双端 preflight manifest 关键字段无 mismatch；`local.yaml` 文件 SHA 与 `network_config_hash` 与手册冻结值一致。

---

## 2. 阶段 Gate 总表

| Gate | 结果 | 关键证据标签 |
|---|---|---|
| 前置 GPU / manifest / UFW | PASS | CudaDevice + SAC/JIT/Buffer4 + 双端 manifest 一致 + Orin `/32` |
| F0 fake 1020/20 | **PASS** | `R10_ACTOR_FAKE: PASS` / `R10_AGENTLACE_REMOTE: PASS` |
| F1 fake 断线 | **PASS** | `R10_SERVER_DISCONNECT: PASS` + `fault_dump/` |
| F2 人工恢复 200/10 | **PASS** | 新 `SERVER_INSTANCE_ID` + 新 Actor `session_id` + `R10_ACTOR_FAKE: PASS` |
| H0 readonly | **PASS** | `R10_ACTOR_READONLY: PASS` |
| H1 live-zero 断线 | **PASS** | `R10_LIVE_DISCONNECT: PASS` |

备注（不改 PASS，但需记录）：

1. H1 最终成功跑在断线前约 **10** 个 zero 步（手册建议 ≥30）；安全项 `STOP_OK/CLEAR_OK`、位移 0、干预 0、`network_loss`、`NO_FURTHER_STEPS`、`AUTO_RECONNECT=false` 均满足。  
2. F0 主跑 `upload_rtt_p95≈2384 ms < 3000`；断线/readonly/live 个别路径出现 p95>3000 的尖峰，以 F0 主通信阈值与 fail-closed 证据为准。  
3. H1 Actor 日志末尾 `SERVER_ENV_COUNT=0` / `LAST_UPDATE_ID_MATCH=FAIL` 是 **Server 已 Ctrl+C 后** 的最后一次对账失败，属预期；Learner `live_disconnect_status.json` 仍记有断线前 `actor_env_count=10`。  
4. 预检文件 `learner_gpu.log` 内容不完整（仅 TF/oneDNN 警告，无 `HIL_LEARNER_GPU: PASS` 行）；**实际 GPU/SAC/JIT 证据以 `learner_gpu_agent.log` + Buffer 以 `learner_gpu_buffer.log` 为准**。  
5. 手册要求的 Orin 宿主机 `ping.txt` / `port_probe.txt` **未归档进本 run 目录**；联机事实已由后续 F0～H1 成功握手与双端口通信间接证明。后续复盘若需补档，应从 Orin 宿主机重新 `ping`/`nc` 并写入同 run。

---

## 3. 冻结值（联机一致）

| 项 | 值 |
|---|---|
| protocol / transition | `wa2-r10-v1` / `r9-v1` |
| Learner IP / Actor IP | `172.16.9.36` / `172.16.8.58` |
| 端口 | TCP 5588 / 5589 |
| `local.yaml` SHA-256 | `88455aa14a54c857cbbb756e61036f569822e561fab67e2af72e368e5f08edf0` |
| network_config_hash | `5a53982b1d8342def2af5632156cc99c372358f6f36de920860436d59c6d7c08` |
| config_bundle_hash | `bff87be7f04e6752ea2c19cec058f5687e51bc9faba15d21aab0535cc2080923` |
| space_hash | `b7a0b860d94b648a56cc453d772c478886dc4d0acecb89d6e8eb527b6831b367` |
| params_tree_signature | `08f79859e3ba2f5da5a9b0cb16e63bd10aa355878e3a0f724465b8850aae7290` |
| source_tree_sha256 | `b3a7537e0b8fb4d6b5cbbc36754478a7462de2b3a9637d098de3f65e657359cb` |
| Agentlace wheel SHA-256 | `1a800cc341f03eb6844273571ba26a265920fa1b5a698acc3d954438cbb72d32` |
| ResNet10 SHA-256 | `175745d43d30233eb01b5369465d1c24c11b8ee71ccb734cc1c1bca13e07f57b` |

双端 preflight manifest 关键字段比对：**无 mismatch**（protocol/task/exp/config/network/space/params/source/wheel/transition）。

---

## 4. Learner 端产出（一句话含义）

根目录：`HILSERL_Learner/runs/...` 与 `runs/r10_preflight/`。

### 4.1 预检

| 产出 | 一句话含义 |
|---|---|
| `r10_preflight/learner_gpu_agent.log` | RTX 4060 上 `CudaDevice(id=0)`、`PARAMS_DEVICES=['cuda:0']`、params signature、`JIT_ACTION=PASS`、`R8_AGENT_INIT: PASS`。 |
| `r10_preflight/learner_gpu_buffer.log` | Buffer 预检 `--insert-count 4`，`R8_REPLAY_BUFFER: PASS`。 |
| `r10_preflight/learner_gpu.log` | 计划中的 `verify_hil_learner_gpu.py` 归档；本文件内容不完整，不单独作为 GPU PASS 证据。 |
| `r10_preflight/learner_manifest.json` | Learner 侧握手指纹（protocol/task/config/space/source/wheel）。 |
| `r10_preflight/actor_manifest.json` | 从 Orin 拷回的 Actor 指纹副本，用于本机比对。 |
| `r10_preflight/ufw_*.txt`；run 内 `firewall.txt` / `ufw_status_numbered.txt` | 防火墙只允许 Orin `172.16.8.58` 访问笔记本 `wlo1` 上 TCP 5588/5589。 |
| `r10_preflight/ports_before_server.txt`；run 内 `port_listen.txt` | Server 启动前后本机端口占用/监听快照。 |

### 4.2 正式联调 `20260814_153513_r10/`

| 产出 | 一句话含义 |
|---|---|
| `learner.log` + `learner_status.json` / `learner_summary.json` | F0 主 Server：握手后收满 **1020/20**，打印 `EXPECT_MET` 与 `R10_AGENTLACE_REMOTE: PASS`。 |
| `disconnect_server.log` + `disconnect_status.json` | F1 故障 Server：收到约 **90** 条后被 Ctrl+C，用于触发 Actor `network_loss`。 |
| `recovery_server.log` + `recovery_status.json` | F2 新 instance：空 store 收满 **200/10**，证明旧 session 未自动续跑。 |
| `readonly_server.log` + `readonly_status.json` | H0 跨机 Server：收真机 readonly transition（本跑 **70** 条），无运动 Gate 的对端。 |
| `live_disconnect_server.log` + `live_disconnect_status.json` | H1 真机零策略断线 Server：握手后收 **10** 条即被停，触发 live fail-closed。 |
| `learner_manifest.json` / `network_config.yaml` | 本 run 归档的握手指纹与局域网 profile 副本（`network_max_age_s: 5.0`）。 |
| `SHA256SUMS` | Learner 本 run 文件完整性清单（20 项）。 |

各次 Server `SERVER_INSTANCE_ID`（均不同，符合「重启必新 instance」）：

```text
F0  551f91ae968a44deb59ac8b61f01af7b
F1  fef539aa167d4299b1f958f68affa1b9
F2  010654bd717e4cb2a5240c6a7ca65e26
H0  153545f0f01749e99de413f862c968b8
H1  1988d8526411456989d7f575aed45616
```

对应 Actor `session_id`（摘自双端 status/JSON）：

```text
F0  20260814T075023Z-e85f0721
F1  20260814T080150Z-34d4f2fd
F2  20260814T080456Z-10546ee3
H0  20260814T082714Z-e3c913a6
H1  20260814T083846Z-e3dff015
```

F0 Learner status 关键字段：

```text
actor_env_count=1020  actor_env_intvn_count=20
last_update_id: actor_env=1019  actor_env_intvn=19
schema_ok=true  handshake_accepted=true  expect_met=true
ordered_digest / ordered_intvn_digest 非空
```

---

## 5. Actor 端产出（一句话含义）

根目录：`catkin_ws/runs/wa2_bottle_pick/20260814_153513_r10/`（同步自 Orin 容器 `/root/catkin_ws/...`）。

| 产出 | 一句话含义 |
|---|---|
| `../r10_preflight/actor_manifest.json` | Actor 侧握手指纹，与 Learner manifest 对齐（不在本 run 子目录内）。 |
| `actor_main.log` / `.json` / `.pkl` | F0 fake 主跑：握手后上传 1020/20（1000 非干预+20 干预），digest/last id 匹配，`R10_ACTOR_FAKE: PASS`。 |
| `actor_disconnect.log` / `.json` | F1：停 Learner 后 `FAULT_REASON=network_loss`，Env/Client 关闭，`R10_SERVER_DISCONNECT: PASS`。 |
| `fault_dump/` | F1 故障现场 dump（`fault_dump.pkl` + `fault_meta.json`），证明不自动重连、可人工事后处理。 |
| `actor_recovery.log` / `.json` | F2：新 session 跑 200/10，无旧 dump 混入，`R10_ACTOR_FAKE: PASS`。 |
| `actor_readonly.log` / `.json` | H0：真图/有限状态、动作忽略、机器人无运动，`R10_ACTOR_READONLY: PASS`。 |
| `actor_live_disconnect.log` / `.json` | H1：zero 策略、干预 0、位移 0，断线后 `STOP_OK/CLEAR_OK`，`R10_LIVE_DISCONNECT: PASS`。 |
| `network_config.yaml` | 本 run 使用的 `local.yaml` 归档副本。 |
| `actor_manifest.json` | 本 run 归档的 Actor manifest 副本。 |
| `SHA256SUMS` | Actor 本 run 文件完整性清单（15 项）。 |

F0 关键数值：

```text
LOCAL/SERVER_ENV=1020  INTVN=20  NON_INTERVENTION=1000  SEGMENTS=1
LAST_UPDATE_ID_MATCH=PASS  ORDERED_DIGEST_MATCH=PASS
NETWORK_UPDATE_COUNT=67  network_age_s≈3.54
handshake_rtt_ms≈5.29
upload_rtt_p95≈2384 ms (<3000)
R10_ACTOR_FAKE: PASS
```

F1 关键数值：

```text
LOCAL_ENV≈90  FAULT_REASON=network_loss  FAULT_DETAIL=server_disconnect
ENV_CLOSED=true  CLIENT_STOPPED=true  AUTO_RECONNECT=false
FAULT_DUMP_WRITTEN=true  NO_FURTHER_STEPS=true
R10_SERVER_DISCONNECT: PASS
```

F2 关键数值：

```text
LOCAL/SERVER_ENV=200  INTVN=10
LAST_UPDATE_ID_MATCH=PASS  ORDERED_DIGEST_MATCH=PASS
NETWORK_UPDATE_COUNT=14  network_age_s≈0.12
新 session ≠ F1 session
R10_ACTOR_FAKE: PASS
```

H0 关键数值：

```text
LOCAL/SERVER_ENV=70  INTVN=0
RESET_MODE=skipped_motion
ROBOT_MOTION=false  REAL_IMAGES=true  STATE_FINITE=true
action_ignored_for_motion=true  readonly_ignored=true
LAST_UPDATE_ID_MATCH=PASS  ORDERED_DIGEST_MATCH=PASS
NETWORK_UPDATE_COUNT=8  network_age_s≈0.05
R10_ACTOR_READONLY: PASS
```

H0 现场输入链：`ACTION_PUBLISHER_EXCLUSIVE: PASS`；Joy 经宿主机 `xhost +SI:localuser:root` 后手动拉起 `spacenavd` + `spacenav_node`（一键脚本曾因 X11 Authorization 卡住，已绕过）；全程不按 SpaceMouse deadman、无 teleop 发布者。

H1 关键数值：

```text
POLICY=zero  INTERVENTION_STEPS=0  translation_m=0  rotation_deg=0
MOTION_WITHOUT_INTERVENTION=false
FAULT_REASON=network_loss  FAULT_DETAIL=server_disconnect
NO_FURTHER_STEPS=true  STOP_OK=true  CLEAR_OK=true
ENV_CLOSED=true  CLIENT_STOPPED=true  AUTO_RECONNECT=false
R10_LIVE_DISCONNECT: PASS
```

---

## 6. 联合判定对照（§6）

### 6.A Learner

- [x] 新根 `/home/cyw/orin_hilserl/HILSERL_Learner` + GPU CudaDevice / params signature / JIT / Buffer 4  
- [x] UFW 仅 Orin `/32` 的 5588/5589（绑定 `wlo1` / `172.16.9.36`）  
- [x] 各 Server 打印 READY + 唯一 instance id  
- [x] F0 status `1020/20`、`schema_ok=true`、last id、ordered digest 非空  
- [x] F1/F2/H0/H1 instance id 互不相同  
- [x] 日志 / JSON / manifest / network / SHA256SUMS 齐全  
- [ ] `ping.txt` / `port_probe.txt` 宿主机归档（**缺失**；不否定联机 PASS，见 §2 备注 5）

### 6.B Actor

- [x] F0 handshake 在 step 前 PASS；1020/20/1000；digest/last id 匹配  
- [x] params callback ≥1，新鲜度 <5 s（F0 `network_age_s≈3.54`）  
- [x] F0 upload p95 <3000 ms  
- [x] F1 `network_loss` + fault dump + 无自动重连  
- [x] F2 新 session 200/10  
- [x] H0 真图、无运动、action ignored  
- [x] H1 zero、干预 0、位移 ≤2 mm、stop/clear  
- [x] Actor 归档与 SHA256SUMS 齐全  

### 6.C 联合

- [x] 同一份 `local.yaml` 语义 / 文件 SHA / `network_config_hash` 一致；白名单 source 一致  
- [x] Learner 先启，Orin 能连通 5588+5589 后启 Actor（由成功握手与双端口通信证实；宿主机探测文本未归档）  
- [x] 停 Learner 后 Actor fail-closed；重启必须人工新 session  

---

## 7. R10 PASS 代表什么 / 不代表什么

**代表：**

```text
跨机协议一致 + fake 1020/20 可核对 + params 可广播
+ 断线 fail-closed + 人工恢复
+ readonly / live-zero 安全 Gate 通过
```

**不代表：**

- 长时间训练性能/温度已合格  
- 已采集可训练 demo（R11）  
- Reward Classifier 可用（R12）  
- 正式 RLPD 已跑（R13）  
- Learner 获得未训练 SAC 的真机 Action 控制权  

下一阶段：**R11 示范采集**（先 5 条 schema，再 ≥20 条成功 demo）。

---

## 8. 现场踩坑摘要（已消化）

| 问题 | 处理 |
|---|---|
| `local.yaml` 中 `network_max_age_s: 5`（int）导致 `network_config_hash` 与已冻结 manifest 不一致 | 改回 `5.0`（float）；文件 SHA 恢复为 `88455aa1…`，无需重建双端 manifest |
| Actor `timeout_ms=3000.0` ZMQ TypeError | 改为 `int(request_timeout_ms)` |
| `$R10_LEARNER_IP` 为空 | 容器必须 export `172.16.9.36` |
| 容器内无 `nc` | 用宿主机 `nc` 或 Python `create_connection` |
| `spacenavd` X11 Authorization | 宿主机 `DISPLAY=:0 xhost +SI:localuser:root`；必要时手动起 `spacenavd`/`spacenav_node`，勿死磕一键脚本卡住 |
| live-zero 20s 提前退出导致无 `network_loss` | `--expect-fault` 时不再按 human `min_seconds` 提前结束 |
| 旧 Server 拒收新 session_id | 每次 Gate 必须新 Server 进程 |
| 手册 `ping.txt`/`port_probe.txt` 未写入本 run | 建议补档；当前不改 PASS 判定 |
