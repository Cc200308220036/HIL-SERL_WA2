# R3 验收记录（2026-08-11）

## 判定

**R3 GATE: PASS** — 可进入 R4（安全 ServoL Action Executor）。

```bash
cd /home/naviai/hilserl_orin
docker exec -i hilserl bash -lc '
source /opt/conda/etc/profile.d/conda.sh && conda activate hil-actor &&
source /ros_noetic/catkin_ws/devel/setup.bash &&
source /opt/ros/noetic/setup.bash --extend &&
source /root/catkin_ws/devel/setup.bash &&
export PYTHONPATH=/root/catkin_ws/src:$PYTHONPATH &&
cd /root/catkin_ws &&
python -m unittest discover -s src/hilserl_wa2/tests/unit -v &&
python src/hilserl_wa2/scripts/verify_r2_mock_env.py &&
python src/hilserl_wa2/scripts/verify_r3_readonly_env.py
'
```

结果摘要：

- 全量单测：**50 passed**
- R2 回归：**R2 GATE: PASS**
- R3 真机只读：**R3 GATE: PASS**（TCP drift=0，stale 钩子通过）
- 10 分钟浸泡：**SOAK: PASS**（27819 steps，RSS 增长 0.3 MB）

## 核心产出

| 路径 | 含义 |
|---|---|
| `ros_adapters/state_monitor.py` | 只订阅 WA2 状态；`StateCache` + `WA2StateMonitor` |
| `envs/wa2_env.py` | 支持 `fake_env=False, read_only=True` |
| `tests/unit/test_state_monitor.py` | 缓存副本/龄期/stale 单测 |
| `tests/unit/test_wa2_env_readonly.py` | 只读 Env 单测 |
| `scripts/verify_r3_readonly_env.py` | 真机只读一键 Gate |
| `scripts/r3_soak_readonly.py` | 10 分钟只读浸泡 |
| `调试日志/阶段验收日志/2026-08-11_R3_soak.txt` | 浸泡原始输出 |
| `调试日志/阶段验收日志/2026-08-11_R3验收_raw.txt` | Gate 原始输出 |
| `docs/R3_方案.md` | 实施方案 |

## 行为冻结

- 观测来自真实 ROS；图像仍 Mock（手腕零图）
- `step(action)` 校验 action，**不发 ServoL**；`action_ignored_for_motion=True`
- 状态超龄 `>0.2s` → `truncated` + `stale`
- 全程机器人静止
