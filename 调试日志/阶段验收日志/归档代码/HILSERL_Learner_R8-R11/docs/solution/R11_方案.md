# R11 方案导航（Learner 树内副本）

R11 **只维护一份**权威方案，避免 Actor/Learner 文档漂移：

```text
笔记本分析根：/home/cyw/orin_hilserl/docs/solution/R11_方案.md
Orin 同步副本：/home/naviai/hilserl_orin/docs/solution/R11_方案.md
```

本文件不是验收依据。验收先看权威方案的 **§2.4 两端分责** 与 **§6.A / §6.B / §6.C**。

运行边界：

| 角色 | 唯一运行根 | 本树内允许启动 |
|---|---|---|
| Learner | `/home/cyw/orin_hilserl/HILSERL_Learner` | `verify_r11_demo_load.py`、`test_r11_demo_schema` |
| Actor | Orin 容器 `/root/catkin_ws` | **禁止**在本树采集 demo 或加载 GPU buffer 冒充 Learner Gate |

Orin 宿主机若同步到本目录，只读，不得 `activate_hil_learner.sh`，不得跑 `record_r11_demos.py`，不得跑 `r10_learner_server.py`。  
R11 加载期不启动 TrainerServer（5588/5589 保持空闲）。  
当前阶段：`R11 GATE: PASS`（验收：`调试日志/阶段验收日志/2026-08-18_R11验收.md`）。
