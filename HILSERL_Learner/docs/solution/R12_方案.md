# R12 方案导航（Learner 树内副本）

R12 **只维护一份**权威方案，避免 Actor/Learner 文档漂移：

```text
笔记本分析根：/home/cyw/orin_hilserl/docs/solution/R12_方案.md
Orin 同步副本：/home/naviai/hilserl_orin/docs/solution/R12_方案.md
```

本文件不是验收依据。验收先看权威方案的 **§2.4 两端分责** 与 **§6.A / §6.B / §6.C**。

运行边界：

| 角色 | 唯一运行根 | 本树内允许启动 |
|---|---|---|
| Learner | `/home/cyw/orin_hilserl/HILSERL_Learner` | `train_r12_reward_classifier.py`、`eval_r12_splits.py`、`test_r12_classifier_schema` |
| Actor | Orin 容器 `/root/catkin_ws` | **禁止**在本树采集图像、加载 GPU ckpt 冒充 Orin 在线抽查、或跑 `record_r12_success_fail.py` |

Orin 宿主机若同步到本目录，只读，不得 `activate_hil_learner.sh`，不得跑 `record_r12_success_fail.py`，不得跑 `r10_learner_server.py`。  
R12 训练期不启动 TrainerServer（5588/5589 保持空闲）。  
R11 的 `demo.pkl` **不是**本阶段数据集。  
当前阶段：`R12 GATE: PASS`（证据见 `调试日志/阶段验收日志/2026-08-19_R12验收.md`，正式 run `0819_1426_r12_clean`）。
