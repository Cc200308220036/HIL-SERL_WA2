# 0825 调试日志

> 工程：HIL-SERL × NaviAI WA2  
> 日期：2026-08-25  
> 主题：夹爪策略塌缩（几乎不合爪）对策  
> R13 run：`20260820_135458_r13`

---

## 1. 现象

- ckpt 已到 ~27000，仍大规模人干预。  
- 策略接近方向趋同；**夹爪几乎不再闭合**。  
- metrics：`temperature≈1e-4`，`rewards` 常 0，`grasp_rewards≈0`，grasp Q 很小。

## 2. 改动（夹爪）

| 项 | 旧 | 新 |
|---|---|---|
| `GRASP_ACTION_PENALTY` | `-0.02` | **`-0.002`**（CLI `--grasp-penalty` 可改） |
| resume cache | 旧 penalty 留在盘里 | 加载后 `_refresh_buffer_grasp_penalties` 按新值重写 |
| `sample_actions` 夹爪 | 训练也硬 `argmax` | HIL 训练 **`grasp_eps=0.15`**：以 ε 探索 **仅开/合**（不含 hold）；评测 `grasp_eps=0` |

涉及文件：

- `HILSERL_Learner/.../r13_learner_train.py`
- `*/hil-serl-main/.../sac_hybrid_single.py`（Learner + Actor 各一份）
- `catkin_ws/.../r13_actor_train.py`（`--grasp-eps`，默认 0.15）
- `catkin_ws/.../r13_actor_eval.py`（显式 `grasp_eps=0`）

## 3. 续训注意

1. **两端都要同步**上述文件后再启。  
2. Learner `--resume`：应看到 `GRASP_ACTION_PENALTY=-0.002`。  
3. Actor：应看到 `GRASP_EPS=0.15`；非干预时偶发开合属预期。  
4. 假阳性分类器仍会污染成功信号，本改不替代补难失败。  
5. 脱手评测必须 `grasp_eps=0`（eval 脚本已固定）。

---

## 4. 臂温度塌缩 / 方向趋同（同日续改）

现状：ckpt ~27k 时 `temperature≈1e-4`（hybrid `temperature_init=1e-2`），臂近乎确定，接近方向趋同。

| 项 | 默认 | 行为 |
|---|---|---|
| `--min-temperature` | **0.05** | 每个 Learner update 后若 α 低于地板则抬回（原 0.01 易贴地欠探索） |
| `--resume-temperature` | **0.05** | `--resume` 且 loaded α &lt; min 时抬到 0.05 |
| 进度条 | `α=…` | 便于观察是否再塌 |

启动期望（新 run 首启无 resume 时，init≈0.01 会被地板抬到 0.05）：

```text
TEMPERATURE_FLOOR min=0.05 resume_kick=0.05 init=0.01
… α=0.05 …
```

参数路径：Flax `ModuleDict` 下为 **`modules_temperature/lagrange`**；写入用 `FrozenDict.copy`（勿全树 unfreeze/freeze）。

---

## 5. 新开干净 Session B run（复用 demo + 分类器）

旧 run `20260820_135458_r13` 可归档；新 run **不要**拷贝旧 `checkpoints/`、`online_buffer_cache/`、扩大后的 `demo_buffer_cache/`。

只复用：

- demo bundle：`wa2_bottle_pick_20_success_7d_7edbdb52`
- 分类器：当前 R13 在用的 `classifier_ckpt`（thr=0.85）

两端重打 manifest；Learner **首启不要 `--resume`**。
