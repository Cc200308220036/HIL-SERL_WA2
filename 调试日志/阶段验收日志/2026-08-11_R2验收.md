# R2 验收记录（2026-08-11）

## 判定

**R2 GATE: PASS** — 可进入 R3（ROS 状态只读 Env）。

```bash
cd /home/naviai/hilserl_orin
docker exec -i hilserl bash -lc '
source /opt/conda/etc/profile.d/conda.sh && conda activate hil-actor &&
export PYTHONPATH=/root/catkin_ws/src:$PYTHONPATH &&
cd /root/catkin_ws &&
python -m unittest discover -s src/hilserl_wa2/tests/unit -v &&
python src/hilserl_wa2/scripts/verify_r2_mock_env.py
'
```

结果摘要：

- 全量单测：**40 passed**（含原 SpaceMouse + 新增 WA2Env）
- `check_env` / 1000 随机步 / fake_env 隔离 / seed 复现 / wrist 零图：**PASS**
- 契约 SHA256：仓库根与包内副本一致（见 `调试日志/阶段验收日志/r2_contract_sha256.txt`）

## 核心产出

| 路径 | 含义 |
|---|---|
| `hilserl_wa2/envs/contracts.py` | 从 YAML 加载契约并构造 Gymnasium spaces |
| `hilserl_wa2/envs/wa2_env.py` | `WA2Env(fake_env=True)` Mock 环境 |
| `hilserl_wa2/ros_adapters/mock_robot.py` | 无 ROS 的左臂状态积分 |
| `hilserl_wa2/ros_adapters/mock_cameras.py` | head/wrist 图像；wrist 固定零图 |
| `hilserl_wa2/configs/wa2_env_contract.yaml` | R1 契约包内副本（容器可读） |
| `hilserl_wa2/tests/unit/test_wa2_*.py` | R2 单测 |
| `hilserl_wa2/scripts/verify_r2_mock_env.py` | 一键 Gate |
| `调试日志/阶段验收日志/2026-08-11_R2验收_raw.txt` | 原始测试输出 |
| `docs/R2_方案.md` | 实施方案 |

## 行为冻结（与 R1 一致）

- action 6D，clip 到 [-1,1]，单步 ≤1 mm / 0.25°
- obs：`state` 四键 + `images.head/wrist`（128×128×3）
- `max_steps=400` → truncated；reward=0
- 无 rospy / 无真机动作
