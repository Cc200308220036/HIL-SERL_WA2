# R10 方案导航（Learner 树内副本）

R10 **只维护一份**权威方案，避免 Actor/Learner 文档漂移：

```text
笔记本分析根：/home/cyw/orin_hilserl/docs/solution/R10_方案.md
Orin 同步副本：/home/naviai/hilserl_orin/docs/solution/R10_方案.md
```

本文件不是验收依据。验收先看权威方案的 **§2.4 两端分责** 与 **§6.A / §6.B / §6.C**。

运行边界：

| 角色 | 唯一运行根 | 本树内允许启动 |
|---|---|---|
| Learner | `/home/cyw/orin_hilserl/HILSERL_Learner` | `r10_learner_server.py`、依赖/GPU 检查 |
| Actor | Orin 容器 `/root/catkin_ws` | **禁止**在本树启动 |

Orin 宿主机若同步到本目录，只读，不得 `activate_hil_learner.sh`。  
当前阶段：`R10 GATE: NOT RUN`。
