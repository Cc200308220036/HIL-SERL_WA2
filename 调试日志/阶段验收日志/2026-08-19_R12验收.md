# R12 验收记录与阶段成果总结

> 落笔日期：2026-08-19  
> 判定：**R12 GATE: PASS**  
> 方案：`docs/solution/R12_方案.md`（含 §5.11.1 FAIL 补数流程）  
> 前置：`调试日志/阶段验收日志/2026-08-18_R11验收.md` → **R11 GATE: PASS**  
> 任务：`bottle_pick` / `wa2_bottle_pick` / 场景 `bottle_desktop`  
> Actor：Orin 容器 `hilserl` / Conda `hil-actor` / `172.16.8.58` / 根 `/root/catkin_ws`  
> Learner：笔记本 `172.16.9.36` / Conda `hil-learner` / Python `3.10.20` / 根 `/home/cyw/orin_hilserl/HILSERL_Learner`  
> 分析根（Orin）：`/home/naviai/hilserl_orin`  
> **正式 Gate run：** `0819_1426_r12_clean`  
> Actor 证据：`catkin_ws/runs/wa2_bottle_pick/0819_1426_r12_clean/`  
> Learner 证据：`HILSERL_Learner/runs/wa2_bottle_pick/0819_1426_r12_clean/`（笔记本同源路径）  
> 早期叠包调试 run `20260818_134757_r12`：**不作为 Gate 证据**（已弃用）

---

## 1. 一句话结论

R12 已用 **人工成功/失败图像快照**（非 R11 `demo.pkl`）在笔记本训出 Reward Classifier，test **P=1.0 / R=1.0**，阈值 **0.85**；ckpt 回 Orin 后真机抽查 **20 帧 FP=FN=0**，且分类器 **未触发 reset**。

```text
R12 GATE: PASS
```

复验依据：正式 clean 包 schema + 训练 `R12_TRAIN: PASS` + `threshold.json`/`metrics.json` + Actor `classifier_ckpt` + `R12_LIVE_EVAL: PASS`（`END_EPISODE=false`，`RESET_CALLED=false`）。

---

## 2. 阶段 Gate 总表（Step 0～10）

| Step | 位置 | 动作 | 结果 | 关键证据 |
|---:|---|---|---|---|
| 0 | 双端 | 共享 IO / wrapper / `classifier_keys` / 单测 | **PASS** | Actor `test_r12_classifier_schema`；`bottle_pick.yaml` → `[head, wrist]` |
| 1 | Actor | fake schema 采集烟测 | **PASS** | 早期 fake/schema 通路已通（本 Gate 以 clean live 为准） |
| 2 | Learner | fake 短训烟测 | **PASS** | `train_r12_reward_classifier.py --smoke` 通路 |
| 3 | Actor | 真机门禁（无 teleop / Joy / R5） | **PASS** | clean 采集前检查；`assert_no_teleop` |
| 4～5 | Actor | 真机采集（最终正式包） | **PASS** | `wa2_bottle_pick_clf_clean_9a7e147d`：成功 100 / 失败 201 / **14 ep** |
| 6 | 双端 | 只 scp 该 clean 包 + SHA | **PASS** | Learner `classifier/wa2_bottle_pick_clf_clean_9a7e147d/` |
| 7 | Learner | 划分 → 训练 → val 阈值 → test 硬门槛 | **PASS** | `R12_TRAIN: PASS`；test P=1.0 R=1.0；thr=0.85 |
| 8 | 双端 | ckpt + `threshold.json` 回 Orin | **PASS** | Actor `classifier_ckpt/checkpoint_150/` + `threshold.json` |
| 9 | Actor | `eval_r12_live.py` 20 标 + 30s hold | **PASS** | `R12_LIVE_EVAL: PASS`；`live_eval.jsonl` 20 行；FP=0 FN=0 |
| 10 | 文档 | 本验收记录 | **PASS** | 本文 |

```text
R12_TRAIN: PASS
R12_LIVE_EVAL: PASS
END_EPISODE=false
RESET_CALLED=false
R12 GATE: PASS
```

过程说明：早期 `20260818_134757_r12` 多轮 merge 曾出现 test P/R 不达标、OOM、划分过脆等问题；最终采用 **新 run + 严标注 clean 单包**（手投影出册=成功；手在册/运动中=失败）过关。失败补数流程已写入方案 **§5.11.1**。

---

## 3. 冻结值与本 run 指纹

| 项 | 值 |
|---|---|
| task / exp / scene | `bottle_pick` / `wa2_bottle_pick` / `bottle_desktop` |
| `classifier_keys` | `["head", "wrist"]` |
| 图像 | `(1,128,128,3)`，无额外 crop |
| `space_hash` | `b7a0b860d94b648a56cc453d772c478886dc4d0acecb89d6e8eb527b6831b367`（与 R11 冻结一致） |
| schema | `r12-v1` |
| 阈值 | **0.85**；`consecutive_n=3`；`val_constraint_ok=true` |
| `end_episode` | **false**（succeed 不得 OR 进 done / reset） |
| Learner / Actor IP | `172.16.9.36` / `172.16.8.58` |
| R12 训练期 Server | **未启** 5588/5589 |
| R11 `demo.pkl` | **未**用作分类器数据 |

正式包：

| 项 | 值 |
|---|---|
| Bundle | `wa2_bottle_pick_clf_clean_9a7e147d` |
| 成功 / 失败 / episode | **100 / 201 / 14** |
| 划分 seed | 12 → train 191 / val 79 / test 31（按 episode，无泄漏） |

---

## 4. 本 run 产物（一句话含义）

路径相对 `.../runs/wa2_bottle_pick/0819_1426_r12_clean/`。

| 产物 | 一句话含义 |
|---|---|
| `classifier/wa2_bottle_pick_clf_clean_9a7e147d/` | 正式训练用成功/失败快照包（`success.pkl`/`failure.pkl` + sidecar + SHA），无 `demo.pkl`。 |
| Learner `splits.json` | episode 级 train/val/test 划分存档（seed=12）。 |
| Learner `metrics.json` | 正式 Gate 指标：test **P=1.0 R=1.0**（tn=21 fp=0 fn=0 tp=10）。 |
| `threshold.json`（双端） | val 约束选出的 **0.85** + `consecutive_n=3`。 |
| `classifier_ckpt/checkpoint_150/` | 可恢复 Flax 分类器权重（150 epoch）。 |
| `live_eval.jsonl` | Step9 真机 20 帧人机对照；**FP=0 FN=0**。 |
| `operator_log.md` / `logs/` | 操作与运行旁证。 |

训练摘要：

```text
THRESHOLD=0.85
VAL_CONSTRAINT_OK=true
TEST_PRECISION=1.0000 TEST_RECALL=1.0000
TEST_CONFUSION tn=21 fp=0 fn=0 tp=10
R12_TRAIN: PASS
```

Step9 摘要：

```text
LABELS=20
FP=0 FN=0
HOLD_SECONDS=30.0
SUCCEED_BURSTS=10
MAX_BURST_S=8.279
END_EPISODE=false
RESET_CALLED=false
R12_LIVE_EVAL: PASS
```

（hold 中 `SUCCEED_BURSTS` 表示画面满足条件时模型多次报成功，**未**触发 reset，符合设计。）

---

## 5. 本阶段新增/改动文件（一句话）

### 5.1 文档

| 文件 | 一句话 |
|---|---|
| `docs/solution/R12_方案.md` | R12 权威方案；含 Gate、两端分责、Step0～10 与 **§5.11.1 FAIL 补数**（唯一操作依据）。 |
| `HILSERL_Learner/docs/solution/R12_方案.md` | Learner 树内导航，指向权威方案。 |

### 5.2 共享语义 / IO

| 文件 | 一句话 |
|---|---|
| `experiments/classifier_io.py`（Actor+Learner） | 分类器样本读写、episode 划分、阈值选择（Fβ）、merge、指标。 |
| `wrappers/reward_classifier.py`（Actor+Learner） | 加载 ckpt 推理；标注 reward/`succeed`；**禁止** `end_episode` 触发 reset。 |
| `experiments/env_factory.py` | 接 `classifier_checkpoint` / 阈值 / `end_episode=false` 包装。 |
| `configs/tasks/bottle_pick.yaml` | `classifier_keys: [head, wrist]`。 |

### 5.3 Actor 脚本 / 测试

| 文件 | 一句话 |
|---|---|
| `scripts/record_r12_success_fail.py` | 真机 tty `s`/`f`/`e` 采快照；右键张合；max_steps 不自动换局。 |
| `scripts/verify_r12_classifier_schema.py` | 离线校验 bundle 数量与 schema。 |
| `scripts/merge_r12_classifier_bundles.py` | 多包合并（补数路径）；episode_id 加前缀防撞。 |
| `scripts/eval_r12_live.py` | Step9 在线抽查；右键张合；忽略 soft max_steps truncate；Orin XLA autotune 关闭。 |
| `tests/unit/test_r12_classifier_schema.py` | R12 IO/划分/wrapper 单测。 |

### 5.4 Learner 脚本

| 文件 | 一句话 |
|---|---|
| `scripts/train_r12_reward_classifier.py` | 正式训练 + val 选阈值 + test Gate；分批评估防 OOM。 |
| `scripts/eval_r12_splits.py` | 对已有 ckpt 复评某一 split。 |
| `scripts/reselect_r12_threshold.py` | 不重训重选阈值；打印 test 扫描与 FP episode。 |
| `scripts/merge_r12_classifier_bundles.py` | 与 Actor 同语义的合并入口（笔记本侧可用）。 |

---

## 6. 明确未做 / 留给 R13

- 未启动 R10 Learner Server / 未跑 RLPD / 未用未训练 SAC 控臂。  
- 分类器 **不** 因 `succeed` 调用 R5 reset（R13 若要 `succeed→done` 须另开连续帧+高阈值方案）。  
- R11 `demo.pkl` 仍只给 R13 `--demo_path`，不是分类器样本。  
- 早期叠包 run 与废弃 clean 包不纳入 Gate 证据。

---

## 7. 验收勾选（方案 §6 摘要）

### 6.A Learner

- [x] 未开 5588/5589；无 ROS  
- [x] 无 R11 `demo.pkl` 作分类器数据  
- [x] clean 包入库；`splits.json` 无 episode 跨集  
- [x] `TEST_PRECISION>=0.85` 且 `TEST_RECALL>=0.80`  
- [x] 阈值来自 val 约束（0.85）  
- [x] `classifier_ckpt/` 可恢复  

### 6.B Actor

- [x] 正式 live clean：≥100/≥200，14 ep  
- [x] ckpt + threshold 已加载抽查  
- [x] `R12_LIVE_EVAL: PASS`；`END_EPISODE=false`；`RESET_CALLED=false`  

### 6.C 联合

- [x] 同一 thr / keys / ckpt 口径  
- [x] 本验收记录落盘  

```text
R12 GATE: PASS
```
