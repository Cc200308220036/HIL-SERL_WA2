# R11 验收记录与阶段成果总结

> 落笔日期：2026-08-18  
> 判定：**R11 GATE: PASS**  
> 方案：`docs/solution/R11_方案.md`  
> 前置：`调试日志/阶段验收日志/2026-08-14_R10验收.md` → **R10 GATE: PASS**  
> 日更（非正式验收）：`调试日志/0817调试日志.md`  
> 运行 ID：`20260817_094325_r11`  
> 任务：`bottle_pick` / `wa2_bottle_pick` / 场景 `bottle_desktop`  
> Actor：Orin 容器 `hilserl` / Conda `hil-actor` / `172.16.8.58` / 根 `/root/catkin_ws`  
> Learner：笔记本 `172.16.9.36` / Conda `hil-learner` / Python `3.10.20` / 根 `/home/cyw/orin_hilserl/HILSERL_Learner`  
> 分析根（Orin）：`/home/naviai/hilserl_orin`  
> Actor 证据：`catkin_ws/runs/wa2_bottle_pick/20260817_094325_r11/`  
> Learner 证据：`HILSERL_Learner/runs/wa2_bottle_pick/20260817_094325_r11/`

---

## 1. 一句话结论

Orin Actor 已用 **zero policy + SpaceMouse 干预** 采到可复现的 `bottle_pick` 成功示范（先 fake5 / live5 schema，再 live20），失败轨迹隔离在 `failed/`；笔记本 Learner 在无 ROS / 无机器人 / 未开 R10 Server 下按 bundle 把三份包插入小容量 demo buffer 并可 sample。正式训练入口仍是该 live20 的 `demo.pkl`，不是 `bundle.json`。

```text
R11 GATE: PASS
```

复验依据：同一 `R11_RUN_ID` 下 Actor `R11_RECORD_FAKE` / `R11_RECORD_LIVE`、Actor `R11_DEMO_SCHEMA`、Learner 三份 `R11_DEMO_LOAD: PASS`、live20 `SHA256SUMS` 双端字节一致、sidecar 全部 `label=success` 且 `intervened_steps>=30`、`hand_toggles>=1`。

---

## 2. 阶段 Gate 总表（Step 0～9）

| Step | 位置 | 动作 | 结果 | 关键证据 |
|---:|---|---|---|---|
| 0 | 双端 | `demo_io` / recorder / load / `request_hand` + 单测 | **PASS** | `test_r11_demo_schema` 离线 6 项；`test_wa2_env.request_hand`；hold-last 后 `test_servo_session` / `test_image_preprocess` / `test_wa2_env` 容器 26 项 OK |
| 1 | Actor | fake 5 条 schema | **PASS** | `logs/record_fake5.log`：`R11_RECORD_FAKE: PASS`，`SUCCESS_EPISODES=5` |
| 2 | Learner | 离线加载 fake5 | **PASS** | `logs/load_fake5.log`：`R11_DEMO_LOAD: PASS`，`BUFFER_CAPACITY=232`，`EPISODES=5` |
| 3 | Actor | 真机门禁（无 teleop / Joy 相机 / R5 reset） | **PASS** | live5/live20 均打印 `episode_box disabled`；每局 `reset done`；recorder `assert_no_teleop` |
| 4 | Actor | 真机 5 条 schema | **PASS** | `logs/record_live5.log`：`R11_RECORD_LIVE: PASS`，成功 5 / 失败 3 |
| 5 | Learner | 加载 live5（真图） | **PASS** | `logs/load_live5.log`：`R11_DEMO_LOAD: PASS`，`BUFFER_CAPACITY=6429`，`SHA256_MATCH=true` |
| 6 | Actor | 真机 ≥20 条成功 | **PASS** | `logs/record_live20.log`：`R11_RECORD_LIVE: PASS`，成功 20 / 本进程失败 22 |
| 7 | 双端 | 只 scp live20 + SHA + Learner 入库 | **PASS** | 双端 `SHA256SUMS` 逐行相同；`logs/load_live20.log`：`R11_DEMO_LOAD: PASS`，`BUFFER_CAPACITY=22009` |
| 8 | Actor | 抽 2 条 TCP / 人工方向（不运动） | **PASS** | `verify_r11_demo_schema.py --episode-index 0,7` → `R11_DEMO_SCHEMA: PASS`；20 条成功抓放与 R7 同号 |
| 9 | 双端 | 写本验收记录 | **PASS** | 本文 |

```text
R11_RECORD_FAKE: PASS
R11_RECORD_LIVE: PASS     # live5 与 live20 各一次
R11_DEMO_SCHEMA: PASS
R11_DEMO_LOAD: PASS       # fake5 / live5 / live20 各一次
R11 GATE: PASS
```

---

## 3. 冻结值与本 run 实际指纹

方案文档冻结、本阶段**未改**的项：

| 项 | 值 |
|---|---|
| task / exp / scene | `bottle_pick` / `wa2_bottle_pick` / `bottle_desktop` |
| action | 6D float32 `[-1,1]`；单步 **1 mm / 0.25°** |
| obs after wrappers | `state (1,27)` / `head,wrist (1,128,128,3)` |
| `episode.max_steps` | 4000 |
| `reward.mode` | `constant_zero`（成功 demo 的 `rewards` 全 0） |
| `setup_mode` | `single-arm-fixed-gripper`（手爪不进 action） |
| transition schema | `r9-v1`（6 键，无 `infos`） |
| `space_hash` | `b7a0b860d94b648a56cc453d772c478886dc4d0acecb89d6e8eb527b6831b367` |
| Learner IP / Actor IP | `172.16.9.36` / `172.16.8.58` |
| policy | **仅 zero**；禁止未训练 SAC 控臂 |

本 run sidecar 实测 `config_bundle_hash`（**相对方案冻结值 `bff87be7…` 已漂移**，因采集当日改了 SpaceMouse 滞回与 ServoL hold-last；**不否定 Gate**）：

| Bundle | `config_bundle_hash` | 包内是否一致 |
|---|---|---|
| fake5 | `668bb92a58d8d597…caee7fbb` | 5/5 相同 |
| live5 | `a862e973ccdc268a…fd89fe93` | 5/5 相同 |
| live20 | `5ff26bd016db9fee…0aed7fab` | **20/20 相同** |

`space_hash` 与 `transition_schema_version=r9-v1` 三包均与冻结值一致。R13 用 live20 时以 **sidecar / `SHA256SUMS` 实值为准**，不要再用方案正文里的旧 `config_bundle_hash` 做字节比对。

---

## 4. 本 run 产物（一句话含义）

路径均相对 `.../runs/wa2_bottle_pick/20260817_094325_r11/`。容器内 Actor 根为 `/root/catkin_ws/runs/...`。

### 4.1 三个成功 bundle（互不覆盖）

| Bundle | 目录 | 一句话含义 |
|---|---|---|
| fake5 | `demos/wa2_bottle_pick_5_fake_908ee479/` | 无硬件合成 5 局，每局 40 步，用于 IO/schema/Learner 加载通路。`demo.pkl`≈38 MB。 |
| live5 | `demos/wa2_bottle_pick_5_live_50ef1664/` | 真机 schema Gate：5 条成功、真图、`intervened≥501`、`hand≥2`。`demo.pkl`≈1.2 GB，6397 步。 |
| live20 | `demos/wa2_bottle_pick_20_success_440eccc2/` | **R13 `--demo_path` 唯一正式包**：20 条成功、21977 步。`demo.pkl`≈4.1 GB。SHA `ec4f7e77649a55d0…e059c734`。 |
| 失败 | Actor `failed/`（25 条 json，含 live5 的 3 条 + live20 进程的 22 条） | 失败/中止只进这里；**Learner 树未拷 `failed/`**。 |

live20 成功 sidecar 共性：`human_success=true`、`reset_ok=true`、`label=success`、最少干预 **373** 步、抓手 **2 或 4** 次、`operator=""`（容器 `$USER` 未设，不改 PASS）。

live20 终端摘要：

```text
R11_RECORD_LIVE: PASS
SUCCESS_EPISODES=20
FAILED_EPISODES=22
MIN_INTERVENED_STEPS_OK=true
HAND_TOGGLES_OK=true
ROBOT_MOTION=true
REAL_IMAGES=true
BUNDLE=.../wa2_bottle_pick_20_success_440eccc2
```

本 live20 进程失败原因（与 `0817调试日志` 一致；磁盘 `failed/` 另含 live5 的 3 条）：

| 原因 | 条数 |
|---|---|
| 操作员 abort / fail | 8 |
| 奇异 | 7 |
| 固件 `PROTECTED`（`cmd_num=15`） | 5 |
| 跟踪误差 >1.5 cm | 1 |
| `servo_faulted` | 1 |

### 4.2 Learner 加载日志

| 产出 | 一句话含义 |
|---|---|
| `logs/load_fake5.log` | fake_env 隔离、buffer=232、sample=8、`R11_DEMO_LOAD: PASS`。当时目录尚无 `SHA256SUMS`（脚本只查文件是否存在），故 `SHA256_MATCH=false`；事后清单已补，`demo.pkl` 哈希与 Actor 一致。 |
| `logs/load_live5.log` | 真图 bundle，buffer=6429，`SHA256_MATCH=true`，`FAILED_DIR_ABSENT=true`。 |
| `logs/load_live20.log` | 正式 20 条：`BUFFER_CAPACITY=22009`（=21977+32，**不是** YAML 的 200000），`PROCESS_DEMOS=identity`，`R11_DEMO_LOAD: PASS`。 |
| 三个 bundle 的 `SHA256SUMS` | 相对路径为 `demo.pkl` / `bundle.json` / `episodes/*.json`。live5/live20 与 Actor 逐行相同。 |

live20 Learner 打印（完整）：

```text
FAKE_ENV_ISOLATED=true
BUFFER_CAPACITY=22009
BUFFER_INSERT_OK=true
SAMPLE_BATCH=8
SCHEMA_OK=true
EPISODES=20
SIDECAR_SUCCESS=20
PROCESS_DEMOS=identity
SHA256_MATCH=true
SAMPLE_BATCH_OK=true
FAILED_DIR_ABSENT=true
R11_DEMO_LOAD: PASS
```

### 4.3 Actor 日志与人工记录

| 产出 | 一句话含义 |
|---|---|
| `logs/record_fake5.log` | 合成 5 条，无真机。 |
| `logs/record_live5.log` | 真机 5 成功 / 3 失败；启动即 `episode_box disabled`。 |
| `logs/record_live20.log` | 真机 20 成功；CTRL 可见 `cmd=SERVOL` 且 `pub_n` 随时间增长（50 Hz hold-last 在跑）。 |
| `operator_log.md` | 人工 s/f/a 流水，同一文件累积 fake/live/debug，**不作自动计数器**。 |
| live20 `episodes/ep000.json`～`ep019.json` | 每条成功局的 sidecar（task/hash/步数/干预/抓手/人工标签）。 |

---

## 5. 联合判定对照（方案 §6）

### 6.A Learner

- [x] 未启动 `r10_learner_server.py`；加载走 `fake_env=True`；`FAKE_ENV_ISOLATED=true`
- [x] fake5 / live5 / live20 **三个 bundle 分别** `R11_DEMO_LOAD: PASS`
- [x] live5 / live20 的 `SHA256SUMS` 与 Actor 一致；相对路径不是整棵 `demos/`
- [x] schema 6 键、无 `infos`、reward 全 0；真机包要求真图（`--require-real-images`）
- [x] `process_demos` 恒等；`MemoryEfficientReplayBufferDataStore` insert 后 `sample(8)`
- [x] Buffer 容量 = `n_transitions+32`（232 / 6429 / 22009），未用 `replay_buffer_capacity=200000`
- [x] 成功条数只来自当前 bundle 的 sidecar / `bundle.json.n_episodes`
- [x] `failed/` 未进 Learner 加载目录
- [x] 未初始化 SAC、未广播 params

### 6.B Actor

- [x] `test_r11_demo_schema` 离线覆盖 6 键 / 失败隔离 / 末步 dones / 干预动作 / reward=0
- [x] fake 5 条 `R11_RECORD_FAKE: PASS`
- [x] 真机采集：无 teleop、R5 开局 reset、`episode_box disabled`、单步 1 mm/0.25° 仍在
- [x] live5 + live20：`R11_RECORD_LIVE: PASS`
- [x] 每步经 `build_actor_transition`；干预覆盖 zero policy
- [x] 成功局 `intervened_steps>=30` 且 `hand_toggles>=1`（live20 最少 373 / 2）
- [x] 失败进 `failed/`，未混入成功 pkl
- [x] 抽 ep0 / ep7：`R11_DEMO_SCHEMA: PASS`；方向与 0810/R7 抓放成功一致
- [x] 归档：pkl、sidecar、`SHA256SUMS`、`record_*.log`、`operator_log.md`

### 6.C 联合

- [x] 同一 `R11_RUN_ID=20260817_094325_r11`
- [x] `space_hash` / `r9-v1` 双端一致；live20 包内 `config_bundle_hash` 一致（相对方案旧冻结值漂移，见 §3）
- [x] live20 `demo.pkl` SHA-256 双端一致（`ec4f7e77…e059c734`）
- [x] Learner 无硬件可独立加载
- [x] 未开未训练 SAC 真机策略，未开 R12 分类器，未跑 RLPD，未开 R10 Server

备注（不改 PASS，但必须记）：

1. **`config_bundle_hash` 相对方案冻结值漂移**，且 fake5 / live5 / live20 三者彼此不同（当日先采 live5，再改滞回/hold-last，再采 live20）。R13 只认 live20 实值。  
2. **fake5 加载时 `SHA256_MATCH=false`** 是校验脚本把「清单文件是否存在」打印成 MATCH，不是哈希算错；清单后补后内容已与 Actor 一致。  
3. sidecar **`operator` 为空**。  
4. Step 8 的 `TCP_DELTA_HEAD` 是整局前 8 步，不是前 8 个干预步；ep0 干预从约 step 200 才开始，前 8 步接近 0 是预期。  
5. live20 整包 unpickle 约 4 GB，16 GB 笔记本需 `python -u` 并避免与大 RSS 进程抢内存；校验容量已用 `n+32`，但 **R13 `train_rlpd.py` 若整包 `pkl.load` 仍会再出现峰值内存问题**（属 R13 加载改造，不回退本 Gate）。  
6. 采集手感未达独立遥操：第一刀是 50 Hz **hold-last 位姿**；按时间积分 latch 是后续项，见 `docs/数据采集优化控制方案.md`。

---

## 6. 本阶段增改代码文件（一句话）

路径默认相对 Actor 包 `catkin_ws/src/hilserl_wa2/`。Learner 树 `HILSERL_Learner/src/hilserl_wa2/` 中的同名文件是共享语义同步副本，不重复解释。

### 6.1 新增（R11 采集 / 校验主干）

| 文件 | 一句话 |
|---|---|
| `experiments/demo_io.py` | 成功 bundle 的 pkl + sidecar + schema 读写，失败隔离，无 ROS/Env 依赖。 |
| `scripts/record_r11_demos.py` | 唯一采集入口：zero policy、左键 toggle、右键经 Env 抓放、人工 s/f/a、满 N 条才写成功包。 |
| `scripts/verify_r11_demo_schema.py` | 不启 Env，按单个 bundle 验 6 键 / 干预下限 / sidecar，并可打印 TCP 增量。 |
| `scripts/verify_r11_demo_load.py` | Learner 离线：fake_env 隔离、`n+32` buffer insert、`process_demos` 恒等、sample，不开 Server/SAC。 |
| `tests/unit/test_r11_demo_schema.py` | 离线单测：6 键无 infos、失败不进成功包、末步 dones、干预动作一致、reward=0。 |

### 6.2 Env / 手爪 / 场景（让 20 条能采完）

| 文件 | 一句话 |
|---|---|
| `envs/wa2_env.py` | 增加 `request_hand` 唯一抓手出口；相机超龄不停 ServoL；`PROTECTED`/奇异/跟踪超差才停积分。 |
| `experiments/env_factory.py` | fake_env 懒加载干预包装，Learner 加载 demo 时不碰 Joy/ROS。 |
| `interventions/end_effector.py` | 抓手 `request_toggle` 后台执行，避免同步 `joint_switch` 堵死 ServoL。 |
| `interventions/wa2_spacemouse_intervention.py` | 左键改为点按进入/退出；Wrapper 只写 `info["sm_right"]`，不直接调手爪 Service。 |
| `envs/scene_config.py` | 单局平移/旋转盒允许 `null`，采集不再被旧 3 cm 盒截断。 |
| `configs/scenes/bottle_desktop.yaml` | `trans_limit_m/rot_limit_deg: null`，`max_steps=4000`；单步上限仍 1 mm/0.25°。 |
| `configs/scenes/_TEMPLATE.yaml` | 新场景模板同步「盒可关、单步不可放」的口径。 |
| `ros_adapters/reset_executor.py` | 复位改为 unlock → stop → clear，避免安全锁上 `stop` 被拒。 |
| `scripts/start_spacemouse_joy.sh` | 后台拉起 `spacenavd`/`spacenav_node`，不占采集终端。 |
| `scripts/verify_r5_reset.py` | 采集开局仍走同一条 `bottle_desktop` R5 reset（Step 8 禁止拿它当真机运动）。 |
| `scripts/verify_r7_intervention_live.py` | 干预真机核验入口与采集 toggle 语义对齐；正式采集时禁止与 recorder 并行。 |

### 6.3 手感 / 卡顿第一刀（未改 1 mm 上限）

| 文件 | 一句话 |
|---|---|
| `interventions/spacemouse_input.py` | 主轴滞回 0.25、换轴不清滤波、近等轴可斜向；平移 XOR 旋转组锁保留。 |
| `interventions/spacemouse_config.py` | 把滞回 / `secondary_axis_ratio` / `session_mode=toggle` 收进 YAML 配置。 |
| `configs/spacemouse/default.yaml` | 采集默认：点按会话 + 滞回 0.25。 |
| `configs/spacemouse/collect.yaml` | 采集档 SpaceMouse 参数，与 default 同源语义。 |
| `interventions/spacemouse_wa2_teleop.py` | 独立遥操同步同一套滞回；**R11 采集进程禁止启动它**（双 ServoL）。 |
| `ros_adapters/servo_session.py` | 50 Hz hold-last 重复发当前 `_cmd_tcp`；识别 `PROTECTED`/`cmd_num=15`；跟踪 >1.5 cm 停积分；`dry_run` 不起线程。 |
| `ros_adapters/image_monitor.py` | rospy 回调只覆盖最新 raw，worker 做 720p→128，降低 GIL 抢占。 |

### 6.4 单测（与上面行为锁死）

| 文件 | 一句话 |
|---|---|
| `tests/unit/test_wa2_env.py` | 补 `request_hand` 在 fake_env 上的出口与 obs 契约。 |
| `tests/unit/test_end_effector.py` | 抓手后台 toggle：不阻塞调用线程。 |
| `tests/unit/test_scene_reset.py` | 场景可关 episode box、fake reset 走 `max_steps=4000`。 |
| `tests/unit/test_wa2_spacemouse_intervention.py` | 左键 toggle 进入后松开仍干预、再点退出。 |
| `tests/unit/test_spacemouse_input.py` | 滞回、次轴抑制、换轴不清滤波、组锁。 |
| `tests/unit/test_spacemouse_config.py` | default/collect YAML 能加载且滞回默认 0.25。 |
| `tests/unit/test_servo_session.py` | hold-last 快于慢 apply、`dry_run` 无发布线程、PROTECTED/跟踪护栏。 |
| `tests/unit/test_image_preprocess.py` | latest-wins raw + worker resize。 |

### 6.5 Learner 同步副本（语义同上，不另解释）

`HILSERL_Learner/src/hilserl_wa2/` 已同步：`experiments/demo_io.py`、`experiments/env_factory.py`、`scripts/verify_r11_demo_load.py`、`tests/unit/test_r11_demo_schema.py`、`envs/wa2_env.py`、`envs/scene_config.py`、`configs/scenes/*`、`interventions/wa2_spacemouse_intervention.py`、`interventions/spacemouse_*.py`、`configs/spacemouse/*`、以及对应单测。ROS adapter（`servo_session` / `image_monitor` / `reset_executor`）与 `record_r11_demos.py` 以 Actor 树为准。

### 6.6 本阶段文档（非代码，但同属 R11 产出）

| 文件 | 一句话 |
|---|---|
| `docs/solution/R11_方案.md` | 本阶段唯一权威方案与分端验收口径。 |
| `HILSERL_Learner/docs/solution/R11_方案.md` | Learner 树导航，指向权威方案，本身不是验收依据。 |
| `docs/数据采集优化控制方案.md` | 卡顿定位、已落地 hold-last、明天 latch+按时间积分的后续刀。 |
| `调试日志/0817调试日志.md` | 采集当日复盘；**不是**正式 Gate。 |
| `docs/README.md` | 增加数据采集优化控制方案索引。 |

**明确未改：** 单步 1 mm / 0.25°；R9 六键 schema；`reward` 仍为 0；禁止失败轨迹拼进成功 pkl；禁止 teleop 与 recorder 双发布。

---

## 7. R11 PASS 代表什么 / 不代表什么

**代表：**

```text
可复现的 bottle_pick 成功示范文件
+ R9 schema 与 R8 space 对齐
+ 笔记本无硬件可加载进 demo buffer
+ 失败轨迹已隔离
```

**不代表：**

- Reward Classifier 可用（R12）  
- 在线 RLPD 已开始（R13）  
- 示范手感已对齐独立遥操（积分仍按 `env.step`，50 Hz 线程只 hold-last）  
- 7D 手部策略已学习（手爪仍是人工边沿）  
- 16 GB 笔记本可轻松整包训练加载 4 GB `demo.pkl`（R13 启动加载需另改）

下一阶段：**R12** 按成功/失败**图像**训 Reward Classifier。R11 的 `demo.pkl` **不是** classifier 数据集，不要混用。R13 才能把 `wa2_bottle_pick_20_success_440eccc2/demo.pkl` 填进 `run_learner.sh --demo_path`。

---

## 8. 现场踩坑摘要（已消化，复跑勿再踩）

| 问题 | 处理 |
|---|---|
| 上游 `record_demos.py` 依赖 classifier / `info["succeed"]` | 改用 `record_r11_demos.py` + 人工 `s/f/a` |
| 旧 3 cm 盒推几下就 truncated | 场景 `trans_limit_m: null`；启动应见 `episode_box disabled` |
| 按住左键、右键同步抓手堵臂 | 左键 toggle；抓手后台；`request_hand` 只经 Env |
| 等 s/f/a 时 watchdog 锁臂 | 询问期间继续 `step(0)` 保活 |
| 主轴 Y/Z 抢轴 | 滞回 0.25，换轴不清滤波 |
| `env.step` 50～140 ms vs 固件 20 ms → `PROTECTED` | 50 Hz hold-last；`cmd_num=15` 停积分；跟踪 >1.5 cm 停 |
| 安全锁上 `stop` 失败 | unlock → stop → clear |
| Ctrl+C 时成功条只在内存 | 必须采满 `--successes-needed` 才写 bundle |
| FIFO stdin 读不了 s/f/a | 采集必须真实 tty |
| glob `*_success_*` 未展开 | 写死 `..._440eccc2` |
| `scp -r demos/` 变成 `demos/demos/` | 只 scp **一个** bundle 目录 |
| Learner 整包 4 GB 看似卡住 | `PYTHONUNBUFFERED=1 python -u`；看 RSS 是否在涨 |
| Step 8 误跑 `verify_r5_reset.py --real` | Step 8 禁止运动 |
| 独立遥操与 recorder 并行 | 立即停；本局失败 |

R14 前仍禁止 `docker rm` / `compose down`。

---

## 9. live20 成功局对照（bundle index → 现场 episode）

抽查用过 **bundle 0 / 7**（现场 ep1 / ep21）。

| bundle | 现场 ep | steps | intervened | hand | started_at (UTC) |
|---:|---:|---:|---:|---:|---|
| 0 | 1 | 1365 | 545 | 2 | 07:51:24Z |
| 1 | 3 | 1560 | 667 | 2 | 07:52:37Z |
| 2 | 6 | 781 | 373 | 2 | 07:54:29Z |
| 3 | 14 | 1365 | 813 | 2 | 07:59:15Z |
| 4 | 17 | 1709 | 1063 | 4 | 08:00:52Z |
| 5 | 18 | 1366 | 955 | 4 | 08:02:09Z |
| 6 | 20 | 1658 | 1168 | 4 | 08:04:12Z |
| 7 | 21 | 1310 | 1006 | 2 | 08:05:48Z |
| 8 | 23 | 1384 | 974 | 4 | 08:08:20Z |
| 9 | 24 | 1058 | 758 | 2 | 08:09:58Z |
| 10 | 26 | 1194 | 801 | 2 | 08:12:24Z |
| 11 | 27 | 1292 | 894 | 2 | 08:13:50Z |
| 12 | 31 | 701 | 527 | 2 | 08:19:19Z |
| 13 | 34 | 673 | 535 | 2 | 08:23:07Z |
| 14 | 35 | 865 | 686 | 2 | 08:24:21Z |
| 15 | 36 | 603 | 467 | 2 | 08:25:55Z |
| 16 | 37 | 915 | 713 | 4 | 08:27:09Z |
| 17 | 39 | 597 | 464 | 2 | 08:30:08Z |
| 18 | 40 | 881 | 740 | 2 | 08:31:23Z |
| 19 | 41 | 700 | 558 | 4 | 08:33:10Z |

合计 **21977** 步。R13 `--demo_path` 写死：

```text
.../20260817_094325_r11/demos/wa2_bottle_pick_20_success_440eccc2/demo.pkl
```
