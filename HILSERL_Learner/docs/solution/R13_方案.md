# R13 方案导航（Learner 树内副本）

R13 **只维护一份**权威方案，避免 Actor/Learner 文档漂移：

```text
笔记本分析根：/home/cyw/orin_hilserl/docs/solution/R13_方案.md
Orin 同步副本：/home/naviai/hilserl_orin/docs/solution/R13_方案.md
```

本文件不是验收依据。验收先看权威方案的 **§2.4** 与 **§6.A / §6.B / §6.C**。

本阶段是 **论文级实验**：训练后脱手完成抓瓶+放瓶；R12 分类器作为稀疏 reward 与成功结束本局的检测器。Gate 是评测 **≥8/10**，不是 fake 数据流。

运行边界：

| 角色 | 唯一运行根 | 本树内允许启动 |
|---|---|---|
| Learner | `/home/cyw/orin_hilserl/HILSERL_Learner` | `r13_learner_train.py`、`verify_r13_demo_load.py`、`test_r13_*` |
| Actor | Orin 容器 `/root/catkin_ws` | **禁止**在本树跑 `r13_actor_train.py` / `r13_actor_eval.py` / `record_r13_demos.py` |

Orin 宿主机若同步到本目录，只读，不得 `activate_hil_learner.sh`。  
Learner 先启 5588/5589，Actor 后启。  
`--demo-path` 用 Actor **当场录制的 7D** bundle（目录或其中的 `demo.pkl` 均可；训练只读 `episodes/epXXX.pkl`），不是 R11 6D 包，也不是离线增广产物，更不是 R12 图像包。  
16GB Learner **禁止**对整包 `demo.pkl` 做 `pickle.load`；缺分局 pkl 时在 Actor 跑 `split_r13_demo_pkl.py`。  
分类器 ckpt 只在 Actor live/eval 加载。  
当前阶段：`R13 GATE: NOT RUN`。
